#[derive(Clone, Copy, Debug, PartialEq, Eq, PartialOrd, Ord)]
pub enum Keyword {
    If,
    Then,
    Else,
    For,
    Do,
    End,
    Public,
    Struct,
    Trait,
    Implements,
    Inherits,
    As,
    Equal,
    Function,
    Mutable,
    Borrow,
    Own,
    In,
    Optional,
    Let,
    Module,
    Derive,
    Match,
    Enum,
    Use,
    ExternCrate
}